# 1. 交叉验证：可靠评估模型

## 🎯 学习目标
- 理解为什么需要交叉验证
- 掌握 K 折交叉验证的原理
- 学会不同交叉验证方法的应用场景
- 能够用交叉验证选择最佳模型

---

## 一、为什么需要交叉验证？

### 问题：单次划分的缺陷

```
传统方法：
数据 → 训练集 (80%) + 测试集 (20%)
      ↓
   训练模型
      ↓
   测试集评估

问题：
✗ 结果依赖随机划分
  不同划分 → 不同结果

✗ 测试集可能不具代表性
  
  好划分：              坏划分：
  │●●●│●○○│           │●●●│●●●│
  │●○○│●○○│           │●○○│●○○│
  训练   测试           训练   测试
  (均衡)              (测试集全正例)

✗ 数据浪费
  测试集数据不用于训练
```

### 解决方案：交叉验证

```
思想：
- 多次划分训练/测试
- 每次都评估
- 取平均结果

好处：
✓ 结果更可靠
✓ 减少随机性影响
✓ 充分利用数据
```

---

## 二、K 折交叉验证

### 原理

```
K 折交叉验证 (K-Fold Cross Validation):

步骤：
1. 将数据随机分成 K 等份
2. 进行 K 次训练和测试：
   - 第 1 次：第 1 份测试，其余 K-1 份训练
   - 第 2 次：第 2 份测试，其余 K-1 份训练
   - ...
   - 第 K 次：第 K 份测试，其余 K-1 份训练
3. 计算 K 次测试的平均结果

可视化 (K=5):

Fold 1: [测试][训练][训练][训练][训练]
Fold 2: [训练][测试][训练][训练][训练]
Fold 3: [训练][训练][测试][训练][训练]
Fold 4: [训练][训练][训练][测试][训练]
Fold 5: [训练][训练][训练][训练][测试]

最后：平均 5 次的结果
```

### K 值选择

```
常见选择：
- K = 5 (常用)
- K = 10 (更可靠)
- K = n (留一法 LOOCV)

K 小 (如 K=3):
✓ 计算快
✗ 方差大 (结果不稳定)

K 大 (如 K=10):
✓ 方差小 (结果稳定)
✗ 计算慢

K = n (留一法):
✓ 最准确
✗ 计算最慢 (n 次训练)
```

### 代码实现

```python
from sklearn.model_selection import cross_val_score, KFold
from sklearn.ensemble import RandomForestClassifier
import numpy as np

# 生成数据
from sklearn.datasets import make_classification
X, y = make_classification(n_samples=1000, n_features=20, 
                           n_informative=10, random_state=42)

# 创建模型
model = RandomForestClassifier(n_estimators=100, random_state=42)

# K 折交叉验证
kfold = KFold(n_splits=5, shuffle=True, random_state=42)

# 执行交叉验证
scores = cross_val_score(model, X, y, cv=kfold, scoring='accuracy')

print(f"各折准确率：{scores}")
print(f"平均准确率：{scores.mean():.4f}")
print(f"标准差：{scores.std():.4f}")
```

### 详细输出

```python
from sklearn.model_selection import cross_validate

# 获取多个指标
scoring = ['accuracy', 'precision', 'recall', 'f1']

scores = cross_validate(
    model, X, y, 
    cv=5, 
    scoring=scoring,
    return_train_score=True
)

# 打印结果
print("交叉验证结果:")
for metric in scoring:
    train_key = f'train_{metric}'
    test_key = f'test_{metric}'
    print(f"\n{metric}:")
    print(f"  训练集：{scores[train_key].mean():.4f} (+/- {scores[train_key].std():.4f})")
    print(f"  测试集：{scores[test_key].mean():.4f} (+/- {scores[test_key].std():.4f})")
```

---

## 三、分层 K 折交叉验证

### 问题：类别不平衡

```
普通 K 折的问题：

原始数据：正例 90%，负例 10%

可能某折：
Fold 1: 正例 100%, 负例 0%  ← 没有负例！

导致：
- 该折无法评估
- 结果偏差大
```

### 解决方案：分层抽样

```
分层 K 折 (Stratified K-Fold):

保证每折的类别比例与原始数据一致

原始数据：正例 90%，负例 10%

分层后：
Fold 1: 正例 90%, 负例 10% ✓
Fold 2: 正例 90%, 负例 10% ✓
...
```

### 代码实现

```python
from sklearn.model_selection import StratifiedKFold

# 创建分层 K 折
skfold = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)

# 执行交叉验证
scores = cross_val_score(model, X, y, cv=skfold, scoring='accuracy')

print(f"分层 K 折准确率：{scores.mean():.4f} (+/- {scores.std():.4f})")
```

---

## 四、其他交叉验证方法

### 1. 留一法 (LOOCV)

```
Leave-One-Out Cross Validation:

每次留 1 个样本测试，其余 n-1 个训练
重复 n 次

优点：
✓ 最准确
✓ 无随机性

缺点：
✗ 计算最慢 (n 次训练)
✗ 方差可能大

适用：小数据集
```

```python
from sklearn.model_selection import LeaveOneOut

loo = LeaveOneOut()
scores = cross_val_score(model, X, y, cv=loo)

print(f"留一法准确率：{scores.mean():.4f}")
```

### 2. 重复 K 折

```
Repeated K-Fold:

K 折交叉验证重复多次

例如：
5 折 × 重复 3 次 = 15 次训练

优点：
✓ 更稳定
✓ 减少随机性

缺点：
✗ 计算更慢

适用：需要非常可靠的评估
```

```python
from sklearn.model_selection import RepeatedKFold

rkfold = RepeatedKFold(n_splits=5, n_repeats=3, random_state=42)
scores = cross_val_score(model, X, y, cv=rkfold)

print(f"重复 K 折准确率：{scores.mean():.4f}")
```

### 3. 时间序列交叉验证

```
Time Series Split:

时间序列数据不能随机打乱！

按时间顺序划分：

Fold 1: [训练 1] [测试 1]
Fold 2: [训练 1+2] [测试 2]
Fold 3: [训练 1+2+3] [测试 3]

训练集只包含过去的信息
```

```python
from sklearn.model_selection import TimeSeriesSplit

tscv = TimeSeriesSplit(n_splits=5)

scores = cross_val_score(model, X, y, cv=tscv)

print(f"时间序列 CV 准确率：{scores.mean():.4f}")
```

---

## 五、用交叉验证选择模型

### 比较不同模型

```python
from sklearn.linear_model import LogisticRegression
from sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier
from sklearn.svm import SVC
from sklearn.naive_bayes import GaussianNB

# 多个模型
models = {
    '逻辑回归': LogisticRegression(random_state=42),
    '随机森林': RandomForestClassifier(random_state=42),
    '梯度提升': GradientBoostingClassifier(random_state=42),
    'SVM': SVC(random_state=42),
    '朴素贝叶斯': GaussianNB()
}

# 交叉验证比较
results = {}
for name, model in models.items():
    scores = cross_val_score(model, X, y, cv=5, scoring='accuracy')
    results[name] = {
        'mean': scores.mean(),
        'std': scores.std()
    }
    print(f"{name}: {scores.mean():.4f} (+/- {scores.std():.4f})")

# 选择最佳模型
best_model = max(results, key=lambda x: results[x]['mean'])
print(f"\n最佳模型：{best_model}")
```

### 可视化比较

```python
import matplotlib.pyplot as plt
import seaborn as sns

# 准备数据
model_names = list(results.keys())
means = [results[name]['mean'] for name in model_names]
stds = [results[name]['std'] for name in model_names]

# 绘制
plt.figure(figsize=(10, 6))
bars = plt.bar(model_names, means, yerr=stds, capsize=5, alpha=0.7)

# 添加数值
for bar, mean in zip(bars, means):
    plt.text(bar.get_x() + bar.get_width()/2, bar.get_height() + 0.01,
             f'{mean:.4f}', ha='center', va='bottom')

plt.xlabel('模型')
plt.ylabel('准确率')
plt.title('模型比较 (5 折交叉验证)')
plt.xticks(rotation=45, ha='right')
plt.ylim(0, 1.1)
plt.tight_layout()
plt.savefig('../images/交叉验证 - 模型比较.png', dpi=150, bbox_inches='tight')
plt.show()
```

---

## 六、用交叉验证调参

### 网格搜索 + 交叉验证

```python
from sklearn.model_selection import GridSearchCV

# 参数网格
param_grid = {
    'n_estimators': [50, 100, 200],
    'max_depth': [None, 10, 20],
    'min_samples_split': [2, 5, 10]
}

# 网格搜索 (内置 5 折交叉验证)
grid_search = GridSearchCV(
    RandomForestClassifier(random_state=42),
    param_grid,
    cv=5,                    # 5 折交叉验证
    scoring='accuracy',
    n_jobs=-1,
    verbose=1
)

grid_search.fit(X, y)

# 最佳参数
print(f"最佳参数：{grid_search.best_params_}")
print(f"最佳分数：{grid_search.best_score_:.4f}")

# 最佳模型
best_model = grid_search.best_estimator_
```

### 随机搜索 + 交叉验证

```python
from sklearn.model_selection import RandomizedSearchCV
from scipy.stats import randint, uniform

# 参数分布
param_dist = {
    'n_estimators': randint(50, 300),
    'max_depth': [None] + list(range(10, 50, 5)),
    'min_samples_split': randint(2, 20),
    'min_samples_leaf': randint(1, 10)
}

# 随机搜索
random_search = RandomizedSearchCV(
    RandomForestClassifier(random_state=42),
    param_distributions=param_dist,
    n_iter=50,               # 尝试 50 组参数
    cv=5,
    scoring='accuracy',
    n_jobs=-1,
    verbose=1
)

random_search.fit(X, y)

print(f"最佳参数：{random_search.best_params_}")
print(f"最佳分数：{random_search.best_score_:.4f}")
```

---

## 七、学习曲线和验证曲线

### 学习曲线

```
学习曲线 = 训练集大小 vs 性能

用途：
- 判断是否过拟合/欠拟合
- 判断是否需要更多数据
```

```python
from sklearn.model_selection import learning_curve

# 计算学习曲线
train_sizes, train_scores, test_scores = learning_curve(
    model, X, y,
    cv=5,
    train_sizes=np.linspace(0.1, 1.0, 10),
    scoring='accuracy',
    n_jobs=-1
)

# 计算均值和标准差
train_mean = np.mean(train_scores, axis=1)
train_std = np.std(train_scores, axis=1)
test_mean = np.mean(test_scores, axis=1)
test_std = np.std(test_scores, axis=1)

# 绘制
plt.figure(figsize=(10, 6))
plt.plot(train_sizes, train_mean, 'o-', label='训练集')
plt.fill_between(train_sizes, train_mean - train_std, 
                 train_mean + train_std, alpha=0.1)
plt.plot(train_sizes, test_mean, 's-', label='测试集')
plt.fill_between(train_sizes, test_mean - test_std, 
                 test_mean + test_std, alpha=0.1)

plt.xlabel('训练样本数')
plt.ylabel('准确率')
plt.title('学习曲线')
plt.legend()
plt.grid(True, alpha=0.3)
plt.savefig('../images/交叉验证 - 学习曲线.png', dpi=150, bbox_inches='tight')
plt.show()
```

### 验证曲线

```
验证曲线 = 单个参数 vs 性能

用途：
- 选择最佳超参数
- 理解参数影响
```

```python
from sklearn.model_selection import validation_curve

# 验证曲线 (改变 n_estimators)
param_range = [10, 50, 100, 200, 300]
train_scores, test_scores = validation_curve(
    RandomForestClassifier(random_state=42),
    X, y,
    param_name='n_estimators',
    param_range=param_range,
    cv=5,
    scoring='accuracy',
    n_jobs=-1
)

# 计算均值和标准差
train_mean = np.mean(train_scores, axis=1)
train_std = np.std(train_scores, axis=1)
test_mean = np.mean(test_scores, axis=1)
test_std = np.std(test_scores, axis=1)

# 绘制
plt.figure(figsize=(10, 6))
plt.plot(param_range, train_mean, 'o-', label='训练集')
plt.fill_between(param_range, train_mean - train_std, 
                 train_mean + train_std, alpha=0.1)
plt.plot(param_range, test_mean, 's-', label='测试集')
plt.fill_between(param_range, test_mean - test_std, 
                 test_mean + test_std, alpha=0.1)

plt.xlabel('n_estimators')
plt.ylabel('准确率')
plt.title('验证曲线 (n_estimators)')
plt.legend()
plt.grid(True, alpha=0.3)
plt.savefig('../images/交叉验证 - 验证曲线.png', dpi=150, bbox_inches='tight')
plt.show()
```

---

## 📝 本章小结

```
┌─────────────────────────────────────────────────────────────┐
│                     核心要点                                │
├─────────────────────────────────────────────────────────────┤
│ • 交叉验证：多次划分，减少随机性影响                        │
│ • K 折 CV: 常用 K=5 或 K=10                                  │
│ • 分层 K 折：保持类别比例，适合不平衡数据                   │
│ • 留一法：最准确但最慢，适合小数据                          │
│ • 时间序列 CV: 按时间顺序，适合时序数据                     │
│ • 网格搜索 + CV: 自动调参                                   │
│ • 学习曲线：判断过拟合/欠拟合，是否需要更多数据             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔜 下一章

[→ 2. 评估指标详解](./02-评估指标.md)

---

*📊 图解机器学习系列 | 模型评估 第 1 章*
